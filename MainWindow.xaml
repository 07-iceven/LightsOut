<Window x:Class="LightsOut.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:LightsOut"
        xmlns:vm="clr-namespace:LightsOut.ViewModels"
        xmlns:models="clr-namespace:LightsOut.Models"
        mc:Ignorable="d"
        Title="LightsOut" 
        Height="650" Width="420"
        WindowStartupLocation="CenterScreen"
        ui:WindowHelper.UseModernWindowStyle="True"
        Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,8,0,16">
            <TextBlock Text="LightsOut" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Master Switch & Countdown -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,10">
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="开启自动化关机" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                    <ui:ToggleSwitch Grid.Column="1"
                                     IsOn="{Binding IsActive}" 
                                     OnContent="{x:Null}" OffContent="{x:Null}" 
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Center"
                                     Width="52"
                                     MinWidth="52"
                                     Margin="0"
                                     Padding="0"/>
                </Grid>
            </Border>

            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="8"
                    Padding="20">
                <TextBlock Text="{Binding CountdownText}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Multi-Time List -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0" 
                     ItemsSource="{Binding ShutdownTimes}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ShutdownTime}">
                        <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                                CornerRadius="8"
                                Padding="16,12">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding DisplayTime}" 
                                           FontSize="22" 
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"
                                           Margin="0,0,0,0"/>

                                <Button Grid.Column="2"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        VerticalAlignment="Center"
                                        Padding="8"
                                        Margin="0,0,10,0"
                                        Command="{Binding DataContext.RemoveTimeCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}">
                                    <Viewbox Width="18" Height="18">
                                        <ui:SymbolIcon Symbol="Delete"/>
                                    </Viewbox>
                                </Button>

                                <ui:ToggleSwitch Grid.Column="3"
                                                 IsOn="{Binding IsEnabled}" 
                                                 OnContent="{x:Null}" OffContent="{x:Null}" 
                                                 VerticalAlignment="Center"
                                                 HorizontalAlignment="Right"
                                                 Width="52"
                                                 MinWidth="52"
                                                 Margin="0"
                                                 Padding="0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Add New Time -->
            <Border Grid.Row="1" 
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ui:NumberBox Value="{Binding NewHour}" 
                                  Header="时"
                                  Minimum="0" Maximum="23"
                                  SpinButtonPlacementMode="Inline"/>
                    
                    <TextBlock Text=":" Grid.Column="1" VerticalAlignment="Bottom" Margin="8,0,8,8" FontSize="20" FontWeight="Bold"/>
                    
                    <ui:NumberBox Grid.Column="2" 
                                  Value="{Binding NewMinute}" 
                                  Header="分"
                                  Minimum="0" Maximum="59"
                                  SpinButtonPlacementMode="Inline"/>

                    <Button Grid.Column="3" 
                               Content="添加" 
                               Command="{Binding AddTimeCommand}"
                               Style="{StaticResource AccentButtonStyle}"
                               VerticalAlignment="Bottom"
                               Margin="10,0,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer / Settings -->
        <Border Grid.Row="3" 
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="8"
                Padding="16"
                Margin="0,10,0,0">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="Setting" Margin="0,0,10,0"/>
                    <TextBlock Text="开机自启动" VerticalAlignment="Center"/>
                </StackPanel>
                <ui:ToggleSwitch Grid.Column="1"
                                 IsOn="{Binding IsStartupEnabled}" 
                                 HorizontalAlignment="Right"
                                 OnContent="{x:Null}" OffContent="{x:Null}" 
                                 Width="52"
                                 MinWidth="52"
                                 Margin="0"
                                 Padding="0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
